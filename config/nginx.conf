server {
    listen       80;
    server_name  .cloudexchange.org;

    location / {
        root   /home/www/domains/cloudexchange.org/app/public;
        index  index.html;
        passenger_enabled on;
    }
}
